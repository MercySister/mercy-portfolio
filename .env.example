# API server
PORT=8080
DATABASE_URL=postgresql://user:password@localhost:5432/mercy_portfolio

# Optional: 163 Mail SMTP notification for new messages
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL=

# Portfolio development server (use these in the frontend process)
# PORT=5173
# BASE_PATH=/
# API_PROXY_TARGET=http://localhost:8080
